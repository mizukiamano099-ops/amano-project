{
  "scripts": {
    "golden:generate": "node tests/golden/generator.js",
    "golden:test": "node tests/golden/runner.js",
    "test:golden": "npm run golden:generate && npm run golden:test"
  }
}

{
  "scripts": {
    "golden:update": "cp -r tests/golden/output/* tests/golden/snapshots/"
  }
}
